<%= form_with model: comment, url: book_comments_path(book.id), method: :post, local: true do |f| %>
  <%= f.text_area :content, row: "5", placeholder: "ここにコメント", class: "comment_form" %>
  <%= f.submit "送信", class: "btn btn-sm btn-success" %>
<% end %>